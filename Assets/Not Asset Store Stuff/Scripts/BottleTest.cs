using AH2714;
using UnityEngine;
public class BottleTest : AH2714.Bottle
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Bottle bottle = new Bottle();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
